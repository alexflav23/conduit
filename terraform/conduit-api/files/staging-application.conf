# Conduit API — staging deploy config. The packaged application.conf is the single source of truth (all values
# env-var driven); this file is what the nixos service module points -Dconfig.file at, so it includes the
# packaged resource and holds only deploy-time literal overrides.
include classpath("application.conf")

hypervolt {
  env = "staging"
}
